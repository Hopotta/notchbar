<Application x:Class="NotchBar.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <LinearGradientBrush x:Key="IslandBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#D8212834" Offset="0" />
            <GradientStop Color="#E3171C26" Offset="0.52" />
            <GradientStop Color="#EA10141D" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="GlassSheen" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#16FFFFFF" Offset="0" />
            <GradientStop Color="#08FFFFFF" Offset="0.42" />
            <GradientStop Color="#00FFFFFF" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PanelBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#28FFFFFF" Offset="0" />
            <GradientStop Color="#12FFFFFF" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="GlassChipBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#30FFFFFF" Offset="0" />
            <GradientStop Color="#18FFFFFF" Offset="1" />
        </LinearGradientBrush>
        <SolidColorBrush x:Key="IslandBorder" Color="#4AFFFFFF" />
        <SolidColorBrush x:Key="GlassEdge" Color="#35FFFFFF" />
        <SolidColorBrush x:Key="InnerHairline" Color="#24FFFFFF" />
        <SolidColorBrush x:Key="PanelBorder" Color="#30FFFFFF" />
        <SolidColorBrush x:Key="SurfaceRaised" Color="#20FFFFFF" />
        <SolidColorBrush x:Key="SurfaceHover" Color="#34FFFFFF" />
        <SolidColorBrush x:Key="SurfacePressed" Color="#46FFFFFF" />
        <SolidColorBrush x:Key="PrimaryText" Color="#F7F9FD" />
        <SolidColorBrush x:Key="SecondaryText" Color="#C2CBD8" />
        <SolidColorBrush x:Key="MutedText" Color="#8D99AA" />
        <SolidColorBrush x:Key="Accent" Color="#8DE7BC" />
        <SolidColorBrush x:Key="AccentSoft" Color="#2A8DE7BC" />
        <SolidColorBrush x:Key="BuiltInAccent" Color="#A4B2C6" />
        <SolidColorBrush x:Key="BuiltInAccentSoft" Color="#24A4B2C6" />
        <SolidColorBrush x:Key="AccentBlue" Color="#94BAFF" />
        <SolidColorBrush x:Key="NotificationAccent" Color="#F2C978" />
        <SolidColorBrush x:Key="NotificationAccentSoft" Color="#2CF2C978" />
        <SolidColorBrush x:Key="ProgressTrack" Color="#2AFFFFFF" />

        <DropShadowEffect x:Key="IslandDepthShadow"
                          Color="#05070D"
                          BlurRadius="18"
                          ShadowDepth="2"
                          Direction="270"
                          Opacity="0.34" />

        <Style x:Key="CompactButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SurfaceHover}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource GlassEdge}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SurfacePressed}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IslandProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="5" />
            <Setter Property="Foreground" Value="{DynamicResource AccentBlue}" />
            <Setter Property="Background" Value="{DynamicResource ProgressTrack}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="PART_Track" ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="3" />
                            <Grid x:Name="PART_Indicator"
                                  HorizontalAlignment="Left">
                                <Border Background="{TemplateBinding Foreground}"
                                        CornerRadius="3" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
